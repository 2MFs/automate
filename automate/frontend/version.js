// Auto-bumped alongside automate/version.py at release time. Loaded
// before app.js so the SPA can show its own version even in local mode
// (when there's no hub to query for /api/status).
window.AUTOMATE_VERSION = "4.5.7";
